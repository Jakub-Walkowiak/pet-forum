import NavbarSideCompact from "@/components/navbar-side-compact";
import NavbarSideFull from "@/components/navbar-side-full";
import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
    title: "Pet Forum",
    description: "Generated by create next app",
};

export default function RootLayout({
    children,
}: Readonly<{
    children: React.ReactNode;
}>) {
    return (
        <html lang="en">
            <body className={inter.className}>
                <main className="mx-auto md:w-5/6 max-w-6xl flex">
                    <NavbarSideCompact></NavbarSideCompact>
                    <NavbarSideFull></NavbarSideFull>
                    <section className="grow p-4 border-s md:border-e border-zinc-700 min-h-screen">{children}</section>
                </main>
            </body>
        </html>
    );
}
